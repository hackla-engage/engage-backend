version: '3'

services:
  web:
    build: .
    command: python /code/manage.py runserver 0.0.0.0:8000 --settings=CouncilTag.docker_settings
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
  db:
    image: postgres
